@model UIMvc.Models.Product

<div class="product-card border rounded-lg p-4 shadow-sm">
  <img src="@Model.ImageUrl" alt="@Model.Name" class="w-full h-48 object-cover rounded" />
  <h2 class="text-xl font-semibold mt-2">@Model.Name</h2>
  <p class="text-gray-700">@Model.Price.ToString("C0")</p>
  <p class="text-sm text-gray-600">@Model.Description</p>
  <button class="mt-3 px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700">Savatga qo‘shish</button>
</div>
