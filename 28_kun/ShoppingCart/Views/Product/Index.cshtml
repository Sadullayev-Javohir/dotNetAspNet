@model List<ShoppingCart.Models.Product>

<h2>Mahsulotlar</h2>
@foreach (var p in Model)
{
  <div>
    <b>@p.Name</b> - $@p.Price
    <a asp-controller="Cart" asp-action="AddToCart" asp-route-id="@p.Id" asp-route-name="@p.Name"
      asp-route-price="@p.Price">
      Savatchaga qo‘shish
    </a>
  </div>
}
